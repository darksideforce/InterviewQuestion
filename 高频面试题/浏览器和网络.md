- 浏览器输入 URL 后发生了什么
	- url解析和缓存检查
	- dns解析、获取服务器ip
	- 建立tcp连接
	- 发送http请求，请求html数据
	- 服务器响应html数据
	- 浏览器解析html数据
	- 断开连接

- http的原理
	- 
- HTTP 1.1、HTTP/2、HTTP/3 的区别
- 强缓存和协商缓存
- `Cache-Control`、`ETag`、`Last-Modified`
- 跨域和 CORS
- 简单请求和预检请求
- Cookie、LocalStorage、SessionStorage、IndexedDB
- XSS 和 CSRF
- 前端鉴权和 Token 刷新
- WebSocket、SSE、长轮询的区别
- 浏览器渲染流程
	- 解析html，构建dom树
	- 解析css，构建css树
	- 合成渲染树
	- 重排
	- 重绘
	- 合成涂层
- 重排和重绘
	- 计算出每个dom节点应该在屏幕上的几何信息，消耗性能较高，移动dom元素会触发
	- 将每个节点的颜色阴影等进行绘制，改变元素颜色而不改变位置则会触发重绘
- 事件冒泡、捕获和事件委托