1:vue3的proxy是什么
proxy是js规范新增的一个原生特性，它本质上是代理对目标对象的所有操作，基于此可以完全取代vue2的object.defineproperty。
proxy通过代理劫持了所有的原生方法，并通过reflect再执行原对象本身的方法。通过get来给vue收集依赖，再通过set来监听所有的修改行为，包括增删属性，并通知vue重新渲染
proxy支持监听对象属性的增改，还可以监听map对象和数组兑现